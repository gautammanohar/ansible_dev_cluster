$TTL    86400
@       IN      SOA     <%= node.name %>. admin.<%= @dns['dns_zone'] %>. (
                  3       ; Serial
             604800     ; Refresh
              86400     ; Retry
            2419200     ; Expire
             604800 )   ; Negative Cache TTL
;
; name servers - NS records
     IN      NS      <%= node.name %>.

; name servers - A records
ns1.<%= @dns['dns_zone'] %>.          IN      A       <%= node['ipaddress'] %>

; <%= node['dns']['network_cidr'] %> - A records


