$TTL    86400
@       IN      SOA     <%= @dns['dns_zone'] %>. admin.<%= @dns['dns_zone'] %>. (
                              3         ; Serial
                         604800         ; Refresh
                          86400         ; Retry
                        2419200         ; Expire
                         604800 )       ; Negative Cache TTL
; name servers
      IN      NS      <%= node.name %>.

; PTR Records

